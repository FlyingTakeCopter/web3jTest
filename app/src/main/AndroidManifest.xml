<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.web3jtest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".MyApplication">
        <activity android:name=".GoodslistActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".shopping.OrderActivity" />

        <activity android:name=".SubscriptionActivity" />

        <activity
            android:name=".MySubscriptionActivity"
            android:launchMode="singleTask" />

        <!--<activity-->
            <!--android:name=".GoodslistActivity"-->
            <!--android:launchMode="singleTask" />-->

        <activity
            android:name=".GoodsRegisterActivity"
            android:launchMode="singleTask" />

        <activity android:name=".GoodsDetailActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".shopping.ProductListActivity">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".shopping.ProductDetailActivity"/>

        <activity android:name=".UserCenterActivity"
            android:launchMode="singleTask"/>
    </application>

</manifest>